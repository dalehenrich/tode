as yet unclassified
catManPage
    ^ 'NAME
    cat - display contents of node

SYNOPSIS
    cat [ path ]

DESCRIPTION
    Resolve `path` and return result of #cat message.

    If `path` is not specified, the #cat message is sent to the 
    current node.
'
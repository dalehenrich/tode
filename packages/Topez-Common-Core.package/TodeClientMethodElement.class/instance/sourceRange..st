as yet unclassified
sourceRange: anArray
  "store as Array instead of Interval, because of bug: https://github.com/svenvc/ston/issues/5"

  "should probably make sure that formatOnDisplay is false"

  sourceRange := anArray
TDTxtLeafNode{#name:'analysis',#contents:Text{#string:'I think the problem is somehow related to the reference objects being handling incorrectly since we are trying to finish up the array .... in fact I think the array is complete so there was an unneccessary push onto the stack somewhere',#runs:RunArray{#runs:[235],#values:[[]],#lastIndex:235,#lastRun:1,#lastOffset:234}}}
TDTxtLeafNode{#name:'analysis',#contents:Text{#string:'
Need to preserve the 3.1 code for ObsoletePathEvaluator ... useful for testing purposes?

GsQueryFormula>>isRangeEqualityOperation: allows lots of invalid operators???

unicode flavors of BtreeQuerySpec ... ready for creating indexed unicode queries (w/o c code support ... I think?) otherwise will have to implement the unicode support in c which isn\'t bad idea',#runs:RunArray{#runs:[363],#values:[[TextColor{#color:Color{#rgb:0}}]],#lastIndex:342,#lastRun:1,#lastOffset:341}}}
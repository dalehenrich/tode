This class allows one to specify the attributes for an unloaded project:

  - projectName
  - version (if applicable)
  - repository location

Using the `project list` browser, one can load the specified project into your image.
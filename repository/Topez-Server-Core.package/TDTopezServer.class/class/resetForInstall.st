accessing
resetForInstall
  "taking out these entries wipes out any customization that may ave been done ... commands, editors, etc. taken back to install condition"

  nodeEditors := nodePrinters := nodeBuilders := halos := commands := nil.
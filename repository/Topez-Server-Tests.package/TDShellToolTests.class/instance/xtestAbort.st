as yet unclassified
xtestAbort
  "can't run this particular test without upsetting the apple cart"

  "tokens command expected"

  self
    executeCommandSpecs:
      {{{'abort'}.
      #'abort'.
      'true'}}
accessing
switchSpecs
  "return an array of switchSpecs."

  "
	 switchSpec (Array):
	   switch state boolean/nil
	   switch action selector
	   switch label
	   switch balloon text
"

  ^ switchSpecs